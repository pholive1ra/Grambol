type WeeklyPromoCardProps = {
  day: string;
  route: string;
  seats: number;
  price: number;
  oldPrice: number;
};

export default function WeeklyPromoCard({
  day,
  route,
  seats,
  price,
  oldPrice,
}: WeeklyPromoCardProps) {
  return;
}
